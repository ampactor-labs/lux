// Crucible 5: Functions are values
// Proves: function references via table indices, indirect call via call_indirect.

fn double(n) = n * 2
fn apply(f, x) = f(x)
println("apply(double, 21) = " ++ to_string(apply(double, 21)))
